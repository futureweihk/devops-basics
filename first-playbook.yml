---
- name: Basic tasks
  hosts: my-localhost
  gather_facts: yes
  tasks:
    - name: Check OS release
      command: cat /etc/os-release
      register: os_result
    - debug: var=os_result.stdout_lines
